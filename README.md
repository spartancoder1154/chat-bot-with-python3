# chat-bot-with-python3
The chatbot reads user input from the command line.
It matches the input against predefined intents (in intents.json).
Based on the match, it returns an appropriate response.
You can extend it to use NLP libraries or machine learning models for smarter responses.
